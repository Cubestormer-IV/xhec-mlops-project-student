# Pre-commit hooks tools
pre-commit

# Code formatting
black

# Import sorting
isort

# Linting tool (code quality checks)
flake8

# YAML and JSON format checkers (for pre-commit hooks)
pyyaml  # To check YAML files
jsonschema  # To check JSON files

# Pre-commit hooks for end-of-file fixes, trailing whitespace, and TOML checks
toml  # For checking TOML files

# Additional dependencies for handling Jupyter notebooks (as specified in pre-commit config)
nbformat  # Jupyter Notebook formatting

